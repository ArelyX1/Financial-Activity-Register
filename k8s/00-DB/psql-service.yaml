apiVersion: v1
kind: Service
metadata:
  name: incasur-psql-service
  namespace: "2023241041"
spec:
  selector:
    app: incasur-db
  ports:
    - protocol: TCP
      port: 5433      # Puerto que usa el servicio
      targetPort: 5433 # Puerto que expone tu Pod
